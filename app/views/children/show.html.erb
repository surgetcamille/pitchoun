<p style="color: green"><%= notice %></p>

<%= render @child %>

<div>
  <%= link_to "Edit this child's profil", edit_child_path(@child) %> |
  <%= link_to "Back to children", children_path %>

  <%= button_to "Delete this child's profil", @child, method: :delete %>
</div>
